import axios from "axios";

// const baseUrl = axios.create({ baseURL: "http://127.0.0.1:8000" });
const baseUrl = axios.create({ baseURL: "https://40ztpaag6l.execute-api.us-east-1.amazonaws.com" });

export default baseUrl;
